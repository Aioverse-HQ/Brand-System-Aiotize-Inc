/**
 * @aiotize/brand-web
 *
 * Main entry point.
 * Re-exports components and utilities from the Aiotize Brand Web package.
 *
 * CSS variables are available as a separate import:
 *   import "@aiotize/brand-web/css";
 */

// Placeholder exports — add real component exports as the package grows.
export const PACKAGE_NAME = "@aiotize/brand-web";
export const VERSION = "1.0.0";
